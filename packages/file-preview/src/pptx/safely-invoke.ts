/**
 * @deprecated Re-exported from `../core/safely-invoke` — import directly from
 * there. This shim will be removed in a future major version.
 */
export { safelyInvoke } from "../core/safely-invoke";

对，既然 15 阶段开始做“稳定性验证”，**直接上 Vitest 更规范**。前面我提的运行时 smoke panel 可以取消，改成真正的测试文件。

Vitest 官方也推荐把它装进项目并通过脚本运行；如果用 Bun，官方特别提醒要用 `bun run test`，不要用 `bun test`，否则会跑 Bun 自己的测试器。([Vitest][1])

## 15 阶段改成 Vitest 方案

### 1. 安装依赖

你的项目现在 `package.json` 里还没有测试脚本，也没有 Vitest 依赖。之前 `scripts` 只有 `dev/build/start/lint/...`。

执行：

```bash id="fggol1"
bun add -D vitest
```

Vitest 官方当前说明是：

```bash id="ictkt8"
bun add -D vitest
```

并且 Vitest 需要 Node >= 20；你的项目本身 Next 16 + React 19，Node 环境要求应该没问题。([Vitest][1])

---

### 2. 修改 `package.json`

新增测试脚本：

```json id="t7l5eq"
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest --watch"
  }
}
```

建议保留 `test` 为 watch 模式，CI 或手动一次性验证用：

```bash id="gctsqx"
bun run test:run
```

Vitest 官方默认会识别 `.test.` 或 `.spec.` 文件名，也推荐用 `vitest run` 做一次性运行。([Vitest][1])

---

### 3. 新增 `vitest.config.ts`

因为你是 Next 项目，不是 Vite 项目，所以建议单独建 `vitest.config.ts`。Vitest 官方说明：没有 Vite 配置时，可以从 `vitest/config` 引入 `defineConfig` 单独配置。([Vitest][2])

新增：

```txt id="t4gkji"
vitest.config.ts
```

内容：

```ts id="ukrxfo"
import path from "node:path";
import { fileURLToPath } from "node:url";
import { defineConfig } from "vitest/config";

const __dirname = path.dirname(fileURLToPath(import.meta.url));

export default defineConfig({
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
    },
  },
  test: {
    environment: "node",
    globals: false,
    include: [
      "src/**/*.test.ts",
      "src/**/*.test.tsx",
      "src/**/*.spec.ts",
      "src/**/*.spec.tsx",
    ],
    exclude: [
      "node_modules",
      ".next",
      "dist",
    ],
  },
});
```

这里先用 `environment: "node"`，因为 15.1 的 registry / support matrix 测试是纯逻辑测试，不需要 DOM。后面如果测 React 组件，再引入 `jsdom + testing-library`。

---

## 4. 新增真正的 registry 测试

新增文件：

```txt id="r4j81u"
src/components/file-preview/__tests__/plugin-registry.test.ts
```

内容：

```ts id="y22g53"
import { describe, expect, it } from "vitest";
import type { FileInfo, FileType } from "../utils";
import { createBuiltinPreviewRegistry } from "../plugins/builtin-plugins";
import { PREVIEW_SUPPORT_MATRIX } from "../support-status";

function createMockFile(fileType: FileType): FileInfo {
  return {
    id: `mock-${fileType}`,
    name: `mock.${fileType}`,
    size: 1,
    type: "",
    fileType,
    content: "mock",
    url: "blob:mock",
    source: undefined,
  };
}

describe("builtin plugin registry", () => {
  it("matches every plugin-supported file type to the expected plugin id", () => {
    const registry = createBuiltinPreviewRegistry();

    for (const meta of Object.values(PREVIEW_SUPPORT_MATRIX)) {
      if (meta.status !== "plugin-supported") continue;

      const plugin = registry.resolve(createMockFile(meta.fileType));

      expect(plugin?.id, `${meta.fileType} should match ${meta.pluginId}`).toBe(
        meta.pluginId
      );
    }
  });

  it("does not match degraded or unsupported file types", () => {
    const registry = createBuiltinPreviewRegistry();

    for (const meta of Object.values(PREVIEW_SUPPORT_MATRIX)) {
      if (meta.status === "plugin-supported") continue;

      const plugin = registry.resolve(createMockFile(meta.fileType));

      expect(plugin, `${meta.fileType} should not match any plugin`).toBeNull();
    }
  });

  it("keeps json routed to the source-code plugin intentionally", () => {
    const registry = createBuiltinPreviewRegistry();

    const plugin = registry.resolve(createMockFile("json"));

    expect(plugin?.id).toBe("builtin.source-code");
  });

  it("keeps legacy office formats out of modern office plugins", () => {
    const registry = createBuiltinPreviewRegistry();

    expect(registry.resolve(createMockFile("doc"))).toBeNull();
    expect(registry.resolve(createMockFile("ppt"))).toBeNull();
    expect(registry.resolve(createMockFile("xls"))).toBeNull();
  });
});
```

这个测试直接覆盖 14 阶段最重要的规则：

```txt id="cus7k2"
plugin-supported 类型必须命中预期 plugin
doc/ppt/xls/unknown 不允许命中 plugin
json 明确复用 builtin.source-code
```

---

## 5. 新增 support matrix 完整性测试

再加一个文件：

```txt id="c1zj96"
src/components/file-preview/__tests__/support-status.test.ts
```

内容：

```ts id="hj6vhg"
import { describe, expect, it } from "vitest";
import type { FileType } from "../utils";
import {
  getPreviewSupportMeta,
  isDegradedFileType,
  isPluginSupportedFileType,
  isUnsupportedFileType,
  PREVIEW_SUPPORT_MATRIX,
} from "../support-status";

const ALL_FILE_TYPES: FileType[] = [
  "pdf",
  "markdown",
  "json",
  "code",
  "docx",
  "doc",
  "pptx",
  "ppt",
  "xlsx",
  "xls",
  "html",
  "zip",
  "svg",
  "rtf",
  "epub",
  "image",
  "text",
  "csv",
  "video",
  "audio",
  "unknown",
];

describe("preview support status matrix", () => {
  it("covers every FileType", () => {
    expect(Object.keys(PREVIEW_SUPPORT_MATRIX).sort()).toEqual(
      [...ALL_FILE_TYPES].sort()
    );
  });

  it("keeps all plugin-supported file types associated with a plugin id", () => {
    for (const meta of Object.values(PREVIEW_SUPPORT_MATRIX)) {
      if (meta.status === "plugin-supported") {
        expect(meta.pluginId, `${meta.fileType} should define pluginId`).toBeTruthy();
        expect(meta.pluginRenderer).toBe("supported");
      }
    }
  });

  it("keeps degraded and unsupported file types without plugin id", () => {
    for (const meta of Object.values(PREVIEW_SUPPORT_MATRIX)) {
      if (meta.status === "degraded" || meta.status === "unsupported") {
        expect(meta.pluginId, `${meta.fileType} should not define pluginId`).toBeNull();
        expect(meta.pluginRenderer).toBe("unsupported");
      }
    }
  });

  it("classifies legacy office formats correctly", () => {
    expect(isDegradedFileType("doc")).toBe(true);
    expect(isUnsupportedFileType("ppt")).toBe(true);
    expect(isUnsupportedFileType("xls")).toBe(true);

    expect(isPluginSupportedFileType("docx")).toBe(true);
    expect(isPluginSupportedFileType("pptx")).toBe(true);
    expect(isPluginSupportedFileType("xlsx")).toBe(true);
  });

  it("returns support metadata by file type", () => {
    expect(getPreviewSupportMeta("pdf").pluginId).toBe("builtin.pdf");
    expect(getPreviewSupportMeta("json").pluginId).toBe("builtin.source-code");
    expect(getPreviewSupportMeta("unknown").status).toBe("unsupported");
  });
});
```

这能防止后续新增 `FileType` 后忘记更新矩阵。

---

## 6. 暂时不测 React 组件

这一阶段我建议先别上：

```txt id="8g9naf"
@testing-library/react
@testing-library/jest-dom
jsdom
```

原因是当前最需要验证的是 registry 和 support matrix，不需要 DOM。等 15.2 或 16 阶段再测 `UnsupportedPluginPreview` 的按钮交互。

如果后面要测组件，再加：

```bash id="g5h8b2"
bun add -D jsdom @testing-library/react @testing-library/jest-dom
```

然后把相关测试文件加：

```ts id="f0sype"
// @vitest-environment jsdom
```

或者在单独 config 里配置 `environment: "jsdom"`。

---

## 7. 本阶段建议删除 smoke panel 方案

也就是说，下面两个文件不要做了：

```txt id="xepc2f"
src/components/file-preview/plugin-smoke-test.ts
src/components/file-preview/PluginSmokeTestPanel.tsx
```

`PluginPreviewRenderer.tsx` 也不要混入测试 UI。

15 阶段改成：

```txt id="lzr7bj"
Vitest 测 registry
Vitest 测 support-status
页面只保留 debug bar
```

这是更干净的工程边界。

---

## 8. 验证命令

本地执行：

```bash id="m58jil"
bun run test:run
```

预期：

```txt id="8fva8j"
2 test files passed
plugin-registry.test.ts passed
support-status.test.ts passed
```

开发时可以用：

```bash id="o8t5sm"
bun run test
```

---

## 推荐提交

```bash id="w9f6cx"
git add package.json
git add bun.lock
git add vitest.config.ts
git add src/components/file-preview/__tests__/plugin-registry.test.ts
git add src/components/file-preview/__tests__/support-status.test.ts
git add docs/preview-plugin-validation-matrix.md

git commit -m "test(preview): add plugin renderer registry tests"
```

15 阶段第一步就按这个做：**先把 registry 和 support matrix 固化成测试**。下一步再考虑大文件提示和组件交互测试。

[1]: https://vitest.dev/guide/ "Getting Started | Guide | Vitest"
[2]: https://vitest.dev/config/ "Configuring Vitest | Vitest"
